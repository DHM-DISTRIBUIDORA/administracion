import { Text, View } from 'react-native'
import React, { Component } from 'react'
import { SList, SView } from 'servisofts-component'

export default class TabSelection extends Component {
    render() {
        return <SView flex border={1}>
            
        </SView>
    }
}