import { Text, View } from 'react-native'
import React, { Component } from 'react'
import { SView } from 'servisofts-component'

export default class MenuBar extends Component {
    render() {
        return <SView col={"xs-12"} height={50} border={1}>

        </SView>
    }
}